const dbConfig = {
  host: "localhost",
  port:3307,
  user: "root",
  password: "",
  database: "stl",
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0,
};

export default dbConfig;
